using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ButtonColour : MonoBehaviour
{

    public ColourByNumber Number;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

//variable to store random range from the start to end of array
//pass the index value to buttons colour renderer 
// store values until scene resets or we leave

// randomise colours in index
// set button colour to random colour 
